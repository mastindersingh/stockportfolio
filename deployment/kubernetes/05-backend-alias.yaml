apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: default
  labels:
    app: stocklive-backend
    service: stocklive-backend-alias
spec:
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: stocklive-backend
  type: ClusterIP